#!/usr/bin/env bash
# No-op stub: Rust git2 is built separately in the feature branch worktree.
# This script exists only to satisfy Xcode build phase checks in main.
echo "[build-rust.sh] No-op stub — Rust build handled in feature branch worktree"
exit 0
